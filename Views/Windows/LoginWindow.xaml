<Window x:Class="UP_Markov.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <Grid>

        <Border Width="350"
                Height="300"
                Background="#2D2D30"
                CornerRadius="15"
                Padding="20">

            <StackPanel>

                <TextBlock Text="Вход"
                           FontSize="28"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 20"/>

                <TextBox x:Name="LoginBox"
                         Height="35"
                         Margin="0 0 0 15"
                         FontSize="16"/>

                <PasswordBox x:Name="PasswordBox"
                             Height="35"
                             Margin="0 0 0 20"
                             FontSize="16"/>

                <Button Content="Войти"
                        Height="40"
                        Click="LoginButton_Click"
                        Margin="0 0 0 10"/>

                <Button Content="Регистрация"
                        Height="40"
                        Click="RegisterButton_Click"/>

            </StackPanel>

        </Border>

    </Grid>

</Window>