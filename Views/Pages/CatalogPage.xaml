<Page x:Class="UP_Markov.Views.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="CatalogPage"
      Background="#1E1E1E">

    <Grid Margin="20">

        <StackPanel>

            <TextBlock Text="Каталог книг"
                       FontSize="32"
                       Foreground="White"
                       Margin="0 0 0 20"/>

            <ListBox x:Name="BooksList"
                     Height="500">

                <ListBox.ItemTemplate>

                    <DataTemplate>

                        <Border BorderBrush="Gray"
                                BorderThickness="1"
                                CornerRadius="10"
                                Margin="5"
                                Padding="10">

                            <StackPanel>

                                <TextBlock Text="{Binding Title}"
                                           FontSize="22"
                                           FontWeight="Bold"/>

                                <TextBlock Text="{Binding Description}"
                                           TextWrapping="Wrap"/>

                            </StackPanel>

                        </Border>

                    </DataTemplate>

                </ListBox.ItemTemplate>

            </ListBox>

        </StackPanel>

    </Grid>

</Page>